'use client';

export default function ({ error }: any) {
  return (
    <p>
      Error in review id. This is Custom Error Page And It caugth only render
      methods not Event errors.
    </p>
  );
}
