export default function () {
  console.log('This is client component');
  return (
    <div>
      This is client component I can render one time on Server and then on
      client side.
    </div>
  );
}
